# FoodLab Studio v0.15.4 上传说明

## 要上传的文件（共 4 个）

把本目录下的 4 个文件上传到仓库 `main` 分支的**根目录**，覆盖同名文件即可：

| 文件 | 状态 | 作用 |
|---|---|---|
| `index.html` | 覆盖 | 统一版本号 + 引入启动保护 |
| `service-worker.js` | 覆盖 | 缓存版本 + 挂载新补丁 |
| `boot-guard-v0154.js` | 新增 | 启动保护（必须先于 app.js 加载） |
| `perf-v0154.js` | 新增 | 渲染限流 |

**必须 4 个一起传**。如果只传部分文件，`index.html` 会引用到不存在的
`boot-guard-v0154.js`，启动保护不会生效（页面仍能打开，但会在控制台报一个
资源加载失败）。

上传后浏览器会提示 `cache cleared for build 0.15.4`，这是清缓存机制第一次
真正生效。

---

## 本次改了什么

### 1. 修复自动清缓存机制（`index.html` 第 9 行）

`FOODLAB_BUILD` 从 `0.14.3` 改为 `0.15.4`。

这段脚本的逻辑是"当这个值变化时，清空所有缓存并强制更新 Service Worker"。
它停在 `0.14.3` 已经跨越了大约 9 个版本，所以这套机制一直没触发过。这是
"文件传上去了但用户看到的还是旧版本"最可能的来源。

同时把 `styles.css` / `app.js` / `chart-fixes.js` / `template-fixes.js` 的
`?v=` 参数从 `0.14.8` 统一到 `0.15.4`，并加了一行清缓存成功的日志。

### 2. 统一版本号（`index.html` + `service-worker.js`）

改之前有 4 处来源、3 个不同数值（`0.14.3` / `0.14.8` / `0.15.3`），而且
v0.14.9 ~ v0.15.3 这 5 个补丁**每个都会各自改写侧边栏的版本号**，谁最后
安装就显示谁。现在统一为 `v0.15.4`，并且由 `boot-guard` 在全部补丁安装完成
之后最后写入一次，不会再被覆盖。

### 3. 新增启动保护 `boot-guard-v0154.js`（必须先于 app.js）

针对 `app.js` 第 156 行的单点故障：

```js
const state = { design: structuredClone(defaultDesign), ... }
```

`app.js` 是一个普通脚本，顶层抛出任何异常都会让**整个文件停止执行**——没有
事件绑定、没有 `init()`、没有补丁链，页面完全空白而且不给任何提示。我在测试
中实际触发过：环境缺少 `structuredClone` 时，`app.js` 在第 156 行中断，后续
全部没有执行。

该文件做三件事：

1. `structuredClone` 兜底实现（含 Date / RegExp / Map / Set / 循环引用处理）
2. 全局错误提示条：脚本错误、资源加载失败、未处理的 Promise 错误都会在页面
   底部显示一条红色提示，写明出错文件和行号，最多显示 3 条，可手动关闭。
   **今后页面不会再无声白屏**，出错时可以直接把提示文字发给我定位。
3. 统一版本号标签

### 4. 新增渲染限流 `perf-v0154.js`

属性面板里每个滑块的 `input` 事件都会触发「完整重算统计 + 整张 SVG 重建」。
拖动一次滑块会连续触发几十次，每次重建对大数据集要几百毫秒。

这个补丁包装了 `renderGalleryStudioCanvas`（Chart Studio）和 `renderChart`
（实验模式）两个渲染入口，采用**自适应冷却**：

- 单次渲染耗时 < 24 ms 时，包装器完全不介入，行为和现在一模一样
- 只有当渲染确实很慢时才开始合并中间的拖动帧
- 冷却时间按实测耗时动态计算（`max(100ms, 上次渲染耗时)`），所以在快电脑、
  慢笔记本、测试环境下的表现一致，不会因为机器不同而失效
- **永远保证最后一次完整渲染**，导出的图和没打补丁时逐字节相同

实测对照（6000 个数据点，模拟拖动 20 帧）：

| | 无补丁 | 有补丁 |
|---|---|---|
| 拖动总耗时 | 9 996 ms | 4 615 ms |
| 中间渲染次数 | 每帧一次 | 合并为 6 次 |
| 最终 SVG 长度 | 850 292 字节 | 850 292 字节 |
| 最终节点数 | 6074 | 6074 |
| 报错数 | 0 | 0 |

最终 SVG 与"强制完整重渲染"的结果**逐字节一致**，这是自动化测试断言过的。

---

## 本次**没有**改的东西

按你说的"不需要改的地方不要改"，以下几项我确认过之后**没有动**：

### 旧版界面残留的 29 个失效 id —— 没有删除

我第一次审查时说"3 个函数一旦调用必定抛错"，这句话是对的，但**结论需要更正**：
`app.js` 第 2117 行有一句

```js
function renderGallery(){ if(!$('#galleryChartList'))return; ... }
```

因为 `#galleryChartList` 已经不存在，这个函数每次都立即返回，后面那些失效 id
根本执行不到。也就是说这批旧代码是**被防御性短路掉的，不是危险代码**。

所以才没有删它——删掉 `app.js` 里的东西收益是零，却要动你最大的那个文件。
如果你以后想清理，可以单独做一轮。

### 其他未改动项

- `app.js`、`chart-fixes.js`、`template-fixes.js`：**一个字节都没改**
- `core.js` 统计引擎：未改
- 所有图形绘制逻辑、配色、默认参数：未改
- `manifest.webmanifest` 里的 `v0.2.1`：未改（它只是安装到手机桌面时的应用
  名称，不影响功能，但确实是第 5 个版本号。要不要一起统一你说了算）
- 大数据集的点抽样：**故意没做**。抽样会改变图上实际画出的点，即使只影响
  显示也可能影响论文插图。需要的话我做成默认关闭的开关。

---

## 验证方式

本次改动不是"改完看着没问题"，而是用无头浏览器环境跑过完整回归：

```bash
node work/audit/harness.mjs
```

测试台会真实复刻 Service Worker 把补丁注入 `template-fixes.js` 的行为，因此脚本
执行顺序与线上完全一致。本次验证覆盖：

- 10 个 JS 文件 + `index.html` 内 7 个内联脚本块的语法校验：全部通过
- 10 种图形（直方图 / KDE / 箱线 / 小提琴 / 散点 / 气泡 / 堆叠 / 饼图 /
  热力图 / 雷达）逐一切换并渲染：0 报错
- 实验模式（柱状图）导入数据并渲染：0 报错
- 未捕获异常：0
- 6000 行数据的拖动 A/B 对照：见上表
- 最终 SVG 与完整重渲染逐字节比对：一致

可以放心上传。

---

## 上传后如何自己确认生效

打开网站，按 F12 打开控制台，应该能看到（顺序不限）：

```
[FoodLab Studio] cache cleared for build 0.15.4      ← 清缓存第一次真正生效
[FoodLab Studio] histogram layer fix v0.15.3 active
[FoodLab Studio] render throttle v0.15.4 active (engages only when a render exceeds 24 ms)
```

侧边栏底部显示 `v0.15.4 · 启动保护与缓存修复版`，并且**不会再变回 v0.15.3**。

想看限流有没有真的在工作，在控制台输入：

```js
FoodLabPerf0154.report
```

返回的 `throttledCalls` 表示"被合并掉的冗余渲染次数"，`trailingRuns` 表示
"收尾补渲染次数"。拖动几下属性面板的滑块再看一次，如果两个数字都增长了，
说明限流正在生效。`slowestRenderMs` 是实测最慢的一次渲染耗时。

如果哪次出现红色错误提示条，把提示文字发我即可，里面包含出错文件和行号。
