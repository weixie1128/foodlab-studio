# FoodLab Studio v0.16.0 上传说明

## 要上传的文件（共 4 个）

上传到仓库 `main` 分支根目录，覆盖同名文件：

| 文件 | 状态 | 作用 |
|---|---|---|
| `index.html` | 覆盖 | 清缓存标记 + 版本号统一到 0.16.0 |
| `service-worker.js` | 覆盖 | 缓存版本 + 挂载新补丁 |
| `boot-guard-v0154.js` | 覆盖 | 启动保护（版本号更新） |
| `series-controls-v0160.js` | 新增 | 本次全部功能 |

`perf-v0154.js` 与上次完全相同，**不用重新上传**。
`app.js`、`chart-fixes.js`、`template-fixes.js`、`core.js` **一个字节都没改**。

---

## 这次解决的三个问题

### 1. 改论文配色会把之前调好的样式全部清空（你说"白调整了"）

这是本次最该修的一项。原因不是一处，而是**三条路径各清一次**：

```
app.js:992      #journalTemplate 监听器  → state.gallery.seriesStyles = {}
app.js:1175     applyTemplate()          → state.chart.seriesStyles = {}
index.html:390  applyGlobalStyle(k,true) → 上面两个都清
```

所以每换一次配色，你调过的线宽、标记大小、标记形状、标记填充、透明度全部被
抹掉，然后从默认值重新生成。

这三个位置都改不动（`applyGlobalStyle` 藏在 index.html 的闭包里，另两个是内联
箭头函数），所以改用**事件括起来**的做法：在配色下拉框上，用捕获阶段先做快照、
用冒泡阶段在**所有**监听器跑完之后再还原，然后重绘画布。不管中间被清了几次，
最后都会还原。

现在的规则就是你要求的那条：

- **系列颜色跟着新配色走**
- 线宽、标记大小、标记形状、标记填充、透明度**全部保留**

另外单独加了「重置全部系列样式」按钮。以前"换配色"和"丢掉我的样式"是同一个
动作，现在拆成两个，想要干净重来时按按钮。

### 2. 标记大小要一个一个系列调

属性面板里"数据系列"下面新增了一个「全部系列」区块：

- 折线图 / 平滑曲线图：全部系列折线粗细、全部系列标记大小
- 所有图库图形：全部系列透明度、线宽/边框、点大小

改一次全部系列同时生效，下面的"本系列"控件仍然保留，需要单独微调时照旧。

### 3. 无法取消标记、无法只显示曲线

以前标记的显示由一条**自动规则**控制（X 轴不同取值超过 120 个就自动隐藏），
而且没有任何开关。现在「全部系列」区块里有三个选项：

| 选项 | 效果 |
|---|---|
| 自动（点多时隐藏标记） | 保持原来的行为 |
| 连线 + 标记 | 强制显示所有标记 |
| 仅连线 | 去掉所有标记，就是你说的"单曲线" |
| 仅标记 | 去掉所有连线，只留数据点 |

选「仅标记」时图例里的线段也会一并去掉，只留标记，不会出现图例和图形不一致。

---

## 验证时额外发现并修掉的一个 bug

这个不在你列的问题里，但属于同一类，顺手修了：

**在柱状图 / 折线图 / 曲线图上切换论文配色会抛 JS 错误。**

原因是两份配色表对不上：

- 下拉框的选项来自 `PALETTE_PRESETS`：`npg / aaas / nejm / lancet / jama /
  jco / tolbright / tolhigh / tolvibrant / tolmuted / okabe / instrument / mono`
- 但 `applyTemplate()` 只认 `templates`：`foodchem / meatsci / nature / mono`

**两张表只有 `mono` 一个重合**。实验模式下走的是 `applyTemplate(选中的值)`，
于是 `templates['npg']` 是 undefined，立刻抛
`Cannot read properties of undefined (reading 'fontEnglish')`。

多元统计之类的图库图形不会犯，因为那条分支本来就写了
`templates[值] || templates.foodchem` 的兜底。现在实验模式也加了同样的兜底。

---

## 这次**没有**做的：坐标轴间隔与对数

你在第一条里提的「横纵坐标无法填写对数、无法自己设置间隔」这次没动，原因是它和
上面三项性质不同：

上面三项可以完全放在一个新的补丁文件里，**不用碰 app.js**。但坐标轴不是——
每个图形的坐标映射（`scaleLinear`）和刻度生成（`makeTicks`）都写在各自绘图
函数的内部，要支持对数就得到每个绘图函数里改，属于会动到出图几何的改动。

这类改动一旦有偏差，画出来的是**错误的科研图**，比卡顿之类的严重得多，所以我不
想混在这一批里一起交。下一轮我打算这样处理：

- 先做「坐标间隔」，纯数值、风险低，覆盖折线/曲线/柱状 + 散点/气泡/直方图/箱线
- 再做「对数轴」，并且加上边界保护：柱状图的对数轴（柱子必须从 0 起）直接禁用并
  给提示，而不是画一张错的图；误差棒跨 0 的情况同样处理
- 每改一个图形，都用同一套无头测试台跑回归，并保证**功能关闭时输出与现在逐字节
  相同**

对数轴你前面已经说过范围（折线/曲线看微生物计数那类跨数量级的数据、散点看吸附
等温线/标准曲线），我按这个范围做，你如果还想加别的图告诉我。

---

## 验证结果

不是"改完看着没问题"，每一项都用无头浏览器实测过：

| 检查项 | 结果 |
|---|---|
| 改配色后实验模式样式保留 | 通过（3 条系列的线宽 5.5 / 标记 11.5 / 菱形 / 同色填充全部保留） |
| 改配色后画布重绘 | 通过（重绘后 SVG 与配色前不同，说明用了新配色） |
| 改配色后系列颜色跟随新配色 | 通过（逐系列比对 palette 索引） |
| 改配色后图库模式非颜色属性保留 | 通过（透明度 0.33 / 点大小 9.5 / 线宽 3.5 / 星形全部保留） |
| 全部系列控件存在且不重复 | 通过（每个面板恰好 1 个区块） |
| 应用到所有系列 | 通过（2 条系列同时变为 13.5） |
| 重置按钮恢复默认 | 通过（13.5 恢复为默认 4） |
| 仅连线模式 | 通过（3 条线、0 个标记） |
| 仅标记模式 | 通过（0 条实线、3 条空路径、12 个标记、图例去掉线段） |
| 10 种图库图形 + 3 种实验图形渲染 | 全部 0 报错 |
| **功能关闭时输出与旧版逐字节相同** | **13/13 全部一致**（10 张图库 + 3 张实验图，SHA-256 比对） |
| 未捕获异常 | 0 |

最后一项是关键：它证明这个补丁在你**没有使用新功能**时，对出图结果零影响。

---

## 上传后怎么用

控制台应该出现：

```
[FoodLab Studio] cache cleared for build 0.16.0
[FoodLab Studio] series controls v0.16.0 active (palette no longer resets per-series styling)
```

侧边栏显示 `v0.16.0 · 系列样式与配色修复版`。

用法：在图中点中任意一条数据系列 → 右侧「对象属性」里，原来的"本系列"控件下面
会多出一个「全部系列」区块。

想确认配色修复生效：调好线宽/标记大小 → 换个论文配色 → 回到该系列看，样式应该
还在，只有颜色变了。
